spring.application.name=OnlineShopping

# JWT配置
jwt.secret=OnlineShoppingSecretKeyForJWTTokenGeneration2024ThisIsALongSecretKeyForSecurity
jwt.expiration=86400000

# 服务器配置
server.port=8080

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://8.163.14.192:3306/onlineshopping?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=onlineshopping
spring.datasource.password=NiERyBeJdwc8NdPB

# ============================================
# JPA配置
# ============================================
# ddl-auto选项说明：
# - update: JPA自动创建/更新表结构（开发环境推荐）
# - validate: 只验证表结构，不修改（生产环境推荐，配合SQL脚本使用）
# - none: 不执行任何操作
# - create: 每次启动删除并重新创建表（慎用）
# - create-drop: 启动时创建，关闭时删除（测试环境）
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================
# SQL脚本自动执行配置
# ============================================
# 启用SQL脚本自动执行
spring.sql.init.mode=always
# 指定建表脚本位置（如果使用JPA自动创建表，可以注释掉这行）
spring.sql.init.schema-locations=classpath:sql/schema.sql
# 指定初始化数据脚本位置
spring.sql.init.data-locations=classpath:sql/data.sql
# 如果表已存在或SQL执行出错，继续执行（不会中断启动）
spring.sql.init.continue-on-error=true
# SQL语句分隔符
spring.sql.init.separator=;
# 平台类型（可选，默认自动检测）
# spring.sql.init.platform=mysql


# 应用基础URL（用于生成邮件中的链接）
app.base-url=http://localhost:8080

# 邮件配置（SMTP）
# 注意：以下配置需要使用环境变量或配置文件管理，不要硬编码敏感信息
# QQ邮箱示例配置：
# spring.mail.host=smtp.qq.com
# spring.mail.port=587
# spring.mail.username=your-email@qq.com
# spring.mail.password=your-email-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.starttls.required=true

# 163邮箱示例配置：
# spring.mail.host=smtp.163.com
# spring.mail.port=465
# spring.mail.username=your-email@163.com
# spring.mail.password=your-email-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.ssl.enable=true

# 开发环境：默认配置（需要根据实际情况配置）
spring.mail.host=smtp.qq.com
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
